{
  "rules": {
    "chats": {
      "$salon": {
        ".read": "auth != null",
        "$messageId": {
          ".write": "auth != null && !data.exists() && newData.child('userId').val() === auth.uid",
          ".validate": "newData.hasChildren(['userId', 'userName', 'texte', 'timestamp']) && newData.child('userName').isString() && newData.child('texte').isString() && newData.child('texte').val().length >= 1 && newData.child('texte').val().length <= 2000 && newData.child('timestamp').isNumber()"
        }
      }
    }
  }
}
